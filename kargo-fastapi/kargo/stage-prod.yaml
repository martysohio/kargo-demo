apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod
  namespace: kargo-fastapi

spec:
  requestedFreight:
    - origin:
        kind: Stage
        name: stage

  promotionTemplate:
    spec:
      steps:
        - uses: git-clone

        - uses: kustomize-set-image
          config:
            path: kargo-fastapi/overlays/prod
            images:
              - image: ghcr.io/martysohio/fastapi-app
                tag: ${{ imageFrom("ghcr.io/martysohio/fastapi-app").Tag }}

        - uses: git-commit

        - uses: git-push